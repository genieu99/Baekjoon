#include <iostream>
#include <string>
using namespace std;

int main() {
    string existedId;
    string surprised = "\?\?!";

    cin >> existedId;
    cout << existedId << surprised << "\n";
}